# Gauss-Legendre Runge-Kutta Methods

```julia; echo=false; results="raw"
for s in 1:3
    show(stdout, "text/markdown", Tableau(Symbol("Gauss($s)"), 2s, get_gauss_coefficients(symtype(), s), get_gauss_weights(symtype(), s), get_gauss_nodes(symtype(), s)))
end
```
