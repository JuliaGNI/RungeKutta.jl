# Gauss-Legendre Runge-Kutta Methods

```julia; echo=false; results="raw"
for s in 1:3
    show(stdout, "text/markdown", Tableau(Symbol("Gauss($s)"), 2s, get_gauss_coefficients(s, Sym), get_gauss_weights(s, Sym), get_gauss_nodes(s, Sym)))
end
```
