# Radau-IIA Runge-Kutta Methods

```julia; echo=false; results="raw"
for s in 2:3
    show(stdout, "text/markdown", Tableau(Symbol("RadauIIA($s)"), 2s-1, get_radau_2_coefficients(s, Sym), get_radau_2_weights(s, Sym), get_radau_2_nodes(s, Sym)))
end
```
